# Methodology Change — 2026-03-18

**Version:** 1.4.0 → 1.5.0

**Change:** Reduced `ESCALATION_SENSITIVITY` from 0.001 to 0.0007.

**Reasoning:** The last change (removing `bias_adjustment`) improved recent-cycle MSE significantly — last two values in the 5-cycle trend are 0.00023 and 0.00019, approaching the naive baseline of 0.000125. Per-market directional accuracy is strong at 0.8333, meaning the direction logic is working; the remaining gap to the naive baseline is a magnitude problem, not a direction problem. Reducing sensitivity by 30% shrinks each shift proportionally, keeping predictions closer to the current price where the naive baseline already excels. This continues the trajectory established by the 0.015→0.001 reduction in v1.2.0.

**Expected effect:** Lower MSE by reducing over-shooting on individual cycles, with minimal impact on directional accuracy since direction is determined by signal sign (unchanged). Target: MSE below 0.00015 within 2–3 cycles.
