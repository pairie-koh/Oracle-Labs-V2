# momentum — 2026-04-02

Version: 1.13.0 -> 1.14.0

The agent's directional accuracy of 9.31% indicates the momentum signal is systematically wrong - predicting the opposite direction of actual market moves. I'm changing MOMENTUM_WEIGHT from positive 0.45 to negative -0.45, which will invert the momentum logic to predict mean reversion instead of trend continuation. This single sign change should dramatically improve directional accuracy since the current logic is so consistently wrong. The magnitude remains the same to preserve the signal strength while fixing the direction.
