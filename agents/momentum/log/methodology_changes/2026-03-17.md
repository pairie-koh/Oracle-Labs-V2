# Methodology Change — 2026-03-17

**Version:** 1.3.0 → 1.4.0
**Change:** Reduced `MOMENTUM_WEIGHT` from 0.3 to 0.15.

Directional accuracy is 0.0 across all 7 predictions, meaning the momentum signal is consistently pointing the wrong direction rather than the right one. Per the iteration rules, when directional accuracy is below 50%, `MOMENTUM_WEIGHT` should be tuned down. The overall MSE (6.732e-05) is already worse than the naive baseline (5.714e-05), and the last cycle spiked to 0.00023131 — 100x worse than the four preceding cycles. Halving `MOMENTUM_WEIGHT` reduces how far the prediction overshoots from the current price, pulling behavior closer to the naive baseline while preserving the sigmoid blend structure for when genuine momentum emerges.
