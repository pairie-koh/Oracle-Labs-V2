# quant — 2026-04-27

Version: 1.34.0 -> 1.35.0

The current momentum-based approach is failing catastrophically with only 8.24% directional accuracy, performing 3.5x worse than the naive baseline. I'm reversing the momentum signal by making the momentum coefficient negative (-0.00001 instead of +0.00001), which will make the model predict moves opposite to recent trends. This addresses the systematic bias that's causing such poor performance across all metrics. The change tests whether recent price momentum is actually a contrarian signal in this market regime.
