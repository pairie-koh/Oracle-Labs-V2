# LLM Forecast Iteration — 2026-04-11

The system shows poor overall performance with 58% accuracy and negative edge vs market (-0.006), indicating systematic overconfidence. Recent performance degraded from SE 0.081 to 0.147 (+0.066). With the LLM consistently losing to market baseline, the current SHRINKAGE_KEEP=0.75 is too aggressive. Reducing to 0.65 will pull LLM predictions closer to market prices, improving calibration while still allowing the system to express conviction when it has genuine edge.

## Replacements Applied

### Change 1
```
# Overconfidence shrinkage: pull LLM estimates toward market price.
# 0.0 = ignore LLM entirely (use market), 1.0 = trust LLM fully (no shrinkage).
# 0.5 = split the difference. Research shows LLMs are systematically overconfident,
# but prediction markets can also be wrong. We keep 75% of LLM divergence from market.
SHRINKAGE_KEEP = 0.75
```
→
```
# Overconfidence shrinkage: pull LLM estimates toward market price.
# 0.0 = ignore LLM entirely (use market), 1.0 = trust LLM fully (no shrinkage).
# 0.5 = split the difference. Research shows LLMs are systematically overconfident,
# but prediction markets can also be wrong. We keep 65% of LLM divergence from market.
SHRINKAGE_KEEP = 0.65
```

